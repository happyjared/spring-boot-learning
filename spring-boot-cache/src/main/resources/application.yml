spring:
  datasource:
    url: jdbc:mysql://192.168.1.165:3306/test?useUnicode=true&zeroDateTimeBehavior=convertToNull&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8&tinyInt1isBit=false
    username: root
    password: winall
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false
  redis:
    host: 59.110.212.17
  cache:
    # 非必须，但如果配置了需补充相应的依赖
    #type: redis
    redis:
      # key 过期时间，单位：毫秒
      time-to-live: 5000
      key-prefix: cn.mariojd.cache.
      cache-null-values: false
